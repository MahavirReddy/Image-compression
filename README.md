# Image-compression
MATLAB-based image compression project using Discrete Cosine Transform (DCT) and JPEG quantization techniques. The system compresses grayscale images, reconstructs them using inverse DCT, and evaluates performance using PSNR, MSE, entropy, compression ratio, and sparsity analysis.
